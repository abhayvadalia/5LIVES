# This first private slice needs no app-owned credentials.
# Sites provides the logical D1 binding DB and verified identity headers.
# Never put secrets in NEXT_PUBLIC_* keys or hosting.json.
# Future public identity, payments, and sender configuration are not active.
# Required names will be added only after choosing a supported provider path.
